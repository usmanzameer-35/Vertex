"use client"
import { useState } from "react"
export function RFQForm() { const [status, setStatus] = useState("idle"); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setStatus("loading"); const response = await fetch("/api/rfq", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(new FormData(event.currentTarget))) }); setStatus(response.ok ? "ready" : "error") } if (status === "ready") return <div className="rounded-xl border border-accent/30 bg-surface p-8"><p className="text-sm font-semibold text-accent">Requirement prepared</p><h2 className="mt-3 font-display text-2xl text-foreground">Connect with Vertex directly.</h2><p className="mt-3 text-sm leading-6 text-muted">The form endpoint validates your requirement but is not connected to an email or CRM service yet. Continue on WhatsApp for a real response.</p><a className="mt-6 inline-block rounded-full bg-accent px-5 py-3 text-sm font-semibold text-background" href="https://wa.me/923333933342">Open WhatsApp</a></div>; return <form className="grid gap-5 rounded-2xl border border-border bg-surface p-6 sm:p-8" onSubmit={submit}><div className="grid gap-5 sm:grid-cols-2">{[["Name", "name", "text"], ["Company", "company", "text"], ["Business email", "email", "email"], ["Phone / WhatsApp", "phone", "tel"]].map(([label, name, type]) => <label className="text-sm font-medium" key={name}>{label}<input className="mt-2 w-full rounded-lg border border-border bg-surface-2 px-4 py-3 outline-none focus:border-accent" name={name} type={type} required /></label>)}</div><label className="text-sm font-medium">Requirement type<select className="mt-2 w-full rounded-lg border border-border bg-surface-2 px-4 py-3" name="type"><option>Product Supply</option><option>Technical Procurement</option><option>Oilfield Support</option><option>Equipment Rental</option><option>Specialist Service</option><option>Facilities / Technology</option></select></label><label className="text-sm font-medium">Item / service description<textarea className="mt-2 w-full rounded-lg border border-border bg-surface-2 px-4 py-3" name="description" rows={5} required /></label><div className="grid gap-5 sm:grid-cols-2">{[["Manufacturer / brand", "brand"], ["Model / part number", "partNumber"], ["Quantity", "quantity"], ["Delivery / site location", "location"], ["Required date", "requiredDate"]].map(([label, name]) => <label className="text-sm font-medium" key={name}>{label}<input className="mt-2 w-full rounded-lg border border-border bg-surface-2 px-4 py-3 outline-none focus:border-accent" name={name} /></label>)}</div><label className="text-sm font-medium">Attach documents<input className="mt-2 block w-full rounded-lg border border-dashed border-border bg-surface-2 p-4 text-sm text-muted" name="files" type="file" multiple accept=".pdf,.jpg,.jpeg,.png,.doc,.docx,.xls,.xlsx" /></label>{status === "error" && <p className="text-sm text-red-400">The requirement could not be validated. Call Vertex directly.</p>}<button className="rounded-full bg-accent px-6 py-3 text-sm font-semibold text-background disabled:opacity-50" disabled={status === "loading"}>{status === "loading" ? "Preparing..." : "Prepare RFQ"}</button></form> }
