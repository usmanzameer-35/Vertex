import { SectionLabel } from "@/components/overview"
import { process } from "@/data/site"
export const metadata = { title: "How We Work" }
export default function ProcessPage() { return <section id="process" className="border-t border-border/60 bg-surface/40"><div className="mx-auto max-w-6xl px-5 py-24"><SectionLabel>Execution</SectionLabel><h1 className="mt-5 max-w-3xl font-display text-5xl font-bold tracking-tight text-foreground">From requirement to handover.</h1><div className="mt-14 grid gap-4 md:grid-cols-5">{process.map(([number, title, text]) => <div className="rounded-xl border border-border bg-surface p-5" key={number}><span className="font-display text-3xl font-bold text-accent">{number}</span><h2 className="mt-10 font-semibold text-foreground">{title}</h2><p className="mt-3 text-sm leading-6 text-muted">{text}</p></div>)}</div><p className="mt-12 max-w-3xl text-xs leading-6 text-muted">These are operating principles and project-specific commitments; they are not presented as independent ISO certification unless such certification is formally obtained.</p></div></section> }
