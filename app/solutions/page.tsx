import Link from "next/link"
import { SectionLabel } from "@/components/overview"
import { capabilities } from "@/data/site"
export const metadata = { title: "Solutions" }
export default function SolutionsPage() { return <section className="border-t border-border/60 bg-surface/40"><div className="mx-auto max-w-6xl px-5 py-24"><SectionLabel>Solutions</SectionLabel><h1 className="mt-5 max-w-3xl font-display text-5xl font-bold tracking-tight text-foreground">Technical support organised around the requirement.</h1><p className="mt-5 max-w-2xl text-base leading-7 text-muted">Vertex connects energy and industrial operations with specification-led procurement, supply and specialist coordination.</p><div className="mt-14 grid gap-5 sm:grid-cols-2">{capabilities.map(([number, title, description, href]) => <Link className="group rounded-2xl border border-border bg-surface p-7 transition hover:border-accent/50" href={href} key={href}><span className="font-mono text-sm text-accent">{number}</span><h2 className="mt-14 font-display text-2xl font-semibold text-foreground">{title}</h2><p className="mt-3 text-sm leading-6 text-muted">{description}</p><span className="mt-7 block text-sm font-semibold text-accent">Explore capability →</span></Link>)}</div></div></section> }
