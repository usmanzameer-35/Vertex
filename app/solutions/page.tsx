import Link from "next/link"
import { SectionLabel } from "@/components/overview"
import { capabilities, secondaryCapabilities } from "@/data/site"
export const metadata = { title: "Solutions" }
export default function SolutionsPage() { return <section className="border-t border-border/60 bg-surface/40"><div className="mx-auto max-w-7xl px-5 py-28 lg:px-8"><SectionLabel>Capabilities</SectionLabel><h1 className="mt-5 max-w-4xl font-display text-5xl font-bold tracking-tight text-foreground sm:text-7xl">Technical support organised around the requirement.</h1><p className="mt-6 max-w-2xl text-base leading-7 text-muted">Vertex connects energy and industrial operations with specification-led procurement, supply and specialist coordination.</p><div className="mt-16 grid gap-px border border-border bg-border sm:grid-cols-2 lg:grid-cols-3">{capabilities.map(([number, title, description, href]) => <Link className="group bg-surface p-7 transition hover:bg-surface-2" href={href} key={href}><span className="font-mono text-sm text-accent">{number}</span><h2 className="mt-16 font-display text-2xl font-semibold text-foreground">{title}</h2><p className="mt-3 text-sm leading-6 text-muted">{description}</p><span className="mt-7 block text-sm font-semibold text-accent">Explore capability <span className="transition group-hover:pl-2">→</span></span></Link>)}</div><div className="mt-12 border-l-2 border-steel pl-5"><p className="text-xs uppercase tracking-[0.2em] text-steel">Secondary division</p>{secondaryCapabilities.map(([title, description, href]) => <Link className="mt-3 block max-w-xl" href={href} key={href}><h2 className="font-display text-xl font-semibold text-foreground">{title}</h2><p className="mt-2 text-sm leading-6 text-muted">{description}</p></Link>)}</div></div></section> }
